---
version: '3'
services:
  buildapp:
    build: buildapp
    volumes:
     - /share:/boxfuse/boxfuse-sample-java-war-hello/target

#  buildtomcat:
#    build: buildtomcat
#    volumes:
#     - /share:/usr/local/tomcat/webapps
#    ports:
#     - "8080:8080"

#  volumes:
#     share:

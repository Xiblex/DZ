---
version: '3'
services:
  buildapp:
    build: buildapp
    volumes:
      - /etc/share:/boxfuse/target

#  buildtomcat:
#    build: buildtomcat
#    volumes:
#     - /share:/usr/local/tomcat/webapps
#    ports:
#     - "8080:8080"

#  volumes:
#     share:
